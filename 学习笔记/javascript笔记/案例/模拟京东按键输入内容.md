**模拟京东按键输入内容**

```
<input type="text">
    <script>
        var seach=document.querySelector('input');
        document.addEventListener('keyup',function(e){
            // if(e.key===83){
            //     seach.focus();
            // }
            if(e.key==='s'){
                seach.focus();
            }
        });
    </script>
```

